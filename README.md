# CSV Parser

Designed to accomidate large csv files through streaming,
such that the entire file does not need to be in memory at once.

Also includes a encode/decoder to map CSV rows onto objects.
Mulitple rows can be decoded/encoded onto slices of objects.
